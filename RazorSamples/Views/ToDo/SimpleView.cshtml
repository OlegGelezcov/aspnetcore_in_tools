@model ToDoItemViewModel
@{
    ViewData["Title"] = "SimpleView";
}

<h1>Tasks to comple</h1>
<ul>
    @for(var i = 0; i < Model.Tasks.Count; i++ ) {
        var task = Model.Tasks[i];
        <li>@i  - @task</li>
    }
</ul>
